# Tasks

## 1. Ask mode

- [ ] 1.1 Driver read-only session (repo + change package readable; all mutation
      tools absent, not merely refused)
- [ ] 1.2 ✦ tab ask mode: bubbles, "Read-only — the AI reads this change and the code,
      but changes nothing" banner, no Stop, no state pill, tab label "✦ Ask AI"
- [ ] 1.3 Source chips on answers (proposal.md → Proposal tab, delta → Spec deltas tab)
- [ ] 1.4 Escalation reply with a Start-a-run button when asked to make changes
- [ ] 1.5 Blocked while a run is active (explains, routes to the run)

## 2. Verify

- [ ] 2.1 Ask about each change type (built, review, draft) — answers cite real sources
- [ ] 2.2 "Just do task 3 for me" produces the escalation, and nothing on disk changed
- [ ] 2.3 Ask → immediately start a run: no ask reply leaks into the run console
