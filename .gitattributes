# Generated by tauri-specta on every build (see src-tauri/src/lib.rs).
# Never hand-merge it: keep our version on conflict and let the next build
# regenerate it. This stops merges/cherry-picks from leaving conflict markers
# in a file Vite watches, which crashes the dev server.
src/lib/bindings.ts merge=ours linguist-generated=true
